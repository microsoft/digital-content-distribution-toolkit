apiVersion: apps/v1
kind: Deployment
metadata:
  name: blendnet-cms-api
  labels:
    app: blendnet-cms-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blendnet-cms-api
  template:
    metadata:
      labels:
        app: blendnet-cms-api
        aadpodidbinding: blendnet-dev-pod-identity
    spec:
      containers:
      - name: blendnet-cms-api
        image: acrblendnetdev.azurecr.io/blendnet.cms.api:0.0.1-20210420.1
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          periodSeconds: 3
          timeoutSeconds: 1
---
apiVersion: v1
kind: Service
metadata:
  name: blendnet-cms-api
spec:
  ports:
  - port: 80
    targetPort : 80
  selector:
    app: blendnet-cms-api